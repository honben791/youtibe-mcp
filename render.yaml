services:
  - type: web
    name: youtube-mcp
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
